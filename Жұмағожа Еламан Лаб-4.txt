# import random

# A = [random.randint(-10, 20) for _ in range(10)]
# print(f"Массив A(10): {A}")

# sum_of_squares = 0
# count_of_positives = 0

# for element in A:
#     if element > 0:
#         sum_of_squares += element ** 2
#         count_of_positives += 1

# if count_of_positives > 0:
#     average_of_squares = sum_of_squares / count_of_positives
#     print(f"Оң элементтердің квадраттарының арифметикалық ортасы: {average_of_squares}")
# else:
#     print("Массивте оң элементтер жоқ.")


# import random

# A = [random.randint(-10, 0) for _ in range(10)]
# print(f"Массив A(10): {A}")

# sum_of_squares = 0
# count_of_positives = 0

# for element in A:
#     if element > 0:
#         sum_of_squares += element ** 2
#         count_of_positives += 1

# if count_of_positives > 0:
#     average_of_squares = sum_of_squares / count_of_positives
#     print(f"Оң элементтердің квадраттарының арифметикалық ортасы: {average_of_squares}")
# else:
    # print("Массивте оң элементтер жоқ.")
    
    
    
import random

A = [[random.randint(1, 10) for _ in range(4)] for _ in range(4)]
print("Массив A(4,4):")
for row in A:
    print(row)

sum_less_than_5 = 0

for row in A:
    for element in row:
        if element < 5:
            sum_less_than_5 += element

print(f"5-тен кіші элементтердің қосындысы: {sum_less_than_5}")